# Bubble Sort

# loop through all elemements of collection (E1)
  # taking E1 from E1 index loop by index through collection length (E2)
    # if E1 > E2 swap elements

# loop through all elements of "collection"
  # starting at index i compare i to i+1
    # if i > i+1 move i+1 to "temp_collection" and delete from "collection"
    # if i < i+1 move i to "temp_collection" and delete from "collection"
  # increment i
# make collection temp_collection